"""The local web application: a browsable, stateful view of the academy.

Nothing in this package is reachable from anywhere but the loopback
interface, and nothing in it authenticates a caller, because there is only
ever one caller. See :mod:`netstack_academy.web.app` for what that costs.
"""
